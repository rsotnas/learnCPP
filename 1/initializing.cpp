#include <iostream>

int main ()
{
  // define an integer variable named x
  int x; // this variable is uninitialized because we haven't given it a value yet

  // print the value of x to the screen
  std::cout << x << '\n';

  return 0;
}